<h1>Edit <%=@heroine.super_name %></h1>

<%= form_for @heroine do |f| %>
<%= form_for @heroine do |f| %>
  <p>Change Secret Identity?: <%= f.text_field :name %></p>
  <p>Change Super Name?: <%=f.text_field :super_name%></p>
  <p>Change Power?: <%= f.collection_select :power_id, @powers, :id, :name %></p>
  <p>Change Image?: <%= f.text_field :img_url %></p>
  <%= f.submit %>
<% end %>
<% end %>
